1. In one sentence: What does res.render(view, data) do?

locate the page file and put the data insider and render it.

2. What is the difference between <%= %> and <%- %>?

<%= %> will convert into html format, but <%- %> will show the file itself, just like the difference between $ and #

3. Where does Express look for EJS templates (folder path)?

views directory.